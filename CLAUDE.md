# Caisse de Poche

Application Flutter **hors-ligne d'abord** de caisse, stock et carnet de dettes, destinée aux petits commerçants. Elle remplace le carnet papier. Android en priorité, web ensuite.

Stockage local : **Hive**. Aucune dépendance réseau pour fonctionner.

---

## Où trouver le plan

- **`docs/PHASES.md`** — le plan de travail complet, Phase 0 à Phase 10. C'est la référence.
- **`docs/PLAN-DESIGN.md`** — l'identité visuelle, à appliquer une fois les phases fonctionnelles terminées.
- **`design/maquette.html`** — maquette validée. Référence pour les couleurs, la typographie et les parcours. Ce n'est pas une source de données.
- **`CHECKLIST.md`** — ce que le propriétaire doit faire manuellement (Firebase, keystore, Play Console).

Lis ces fichiers avant de commencer quoi que ce soit.

---

## État actuel

- **Phase 0** — terminée. Le projet compile.
- **Phase 1** — terminée. Prix d'achat figés dans `ItemVendu`, montants en unités mineures, décimales par devise.
- **Crash au démarrage** — résolu. La cause était `firebase_core` et `google_mobile_ads`, qui s'initialisent côté natif avant Dart et faisaient tomber le processus faute de configuration. **Les deux ont été retirés**, ainsi que `cloud_firestore` et `connectivity_plus`. `SyncService` et `AdService` sont des implémentations neutres qui ne font rien. Les trois APK démarrent correctement sur un appareil réel.
- **Phase 2** — terminée. Export et import de sauvegarde fonctionnent (via `file_selector`, `file_picker` ayant été abandonné), testés en debug et en release sur appareil réel.
- **Phase 3** — terminée. Panier : deux actions, « Encaisser » et « Mettre en dette » (sélection d'un client existant ou saisie rapide). `Transaction.modePaiement` (especes/credit/mobileMoney) et `Dette.transactionId` relient les deux. Nouveau modèle `PaiementDette` (typeId 7) pour l'historique des paiements partiels. Tableau de bord de l'écran Caisse : CA du jour, bénéfice net et encaissements du jour affichés séparément — un paiement de dette n'est jamais recompté comme une vente. Testée sur téléphone : vente au comptant, vente à crédit et distinction CA/encaissements fonctionnent. Corrections après retour terrain : `DetteProvider` n'était pas rechargé après une vente à crédit (la dette n'apparaissait dans le carnet qu'après un rechargement manuel) ; le téléphone du client est désormais optionnel partout (panier « Mettre en dette » et formulaire du carnet de dettes), les boutons WhatsApp/SMS se désactivant proprement si aucun numéro n'est renseigné.
- **Phase 4** — terminée. Annulation et correction de vente : `Transaction.annulee` / `dateAnnulation` / `motifAnnulation` (nouveaux champs additifs, `@HiveField` 10-12), méthode `marquerAnnulee()`. `CaisseService.annulerTransaction()` remet le stock en rayon, marque la transaction annulée sans jamais la supprimer, et l'exclut des statistiques du jour (`transactionsDuJour()` filtre désormais `!annulee`) — mais elle reste consultable dans un nouveau « Journal des ventes » (icône historique dans l'AppBar de l'écran Caisse), y compris les ventes annulées, avec le nom du client affiché pour les ventes à crédit. Pour une vente à crédit : l'annulation supprime la dette liée si aucun paiement n'a encore été reçu, mais est bloquée si un paiement partiel existe déjà (pour ne jamais faire disparaître un encaissement réel). Testée sur téléphone.
- **Phase 5** — terminée (code). Verrouillage par code PIN (4-6 chiffres) au lancement et au retour d'arrière-plan (`WidgetsBindingObserver` dans `main.dart`, `AppLifecycleState.paused` → reverrouille). Nouveau `PinService` (hash SHA-256 salé, jamais le PIN en clair) stocké via `flutter_secure_storage` (nouveau plugin natif, validé explicitement par le propriétaire — pas de biométrie pour l'instant, cf. décision du 2026-07-30). Question secrète pour réinitialiser le PIN en cas d'oubli. Activation/désactivation/changement depuis l'onglet Paramètres — désactivé par défaut, aucune friction ajoutée pour qui ne l'active pas. **Pas encore testée sur téléphone.**
- **Phase 6** — terminée (code). Numéros normalisés en E.164 (`lib/utils/telephone.dart`) à partir de l'indicatif du pays choisi à l'onboarding (`CurrencyProvider.indicatifPays`, stocké dès l'onboarding via `country_picker` et avec un repli par pays pour les installations antérieures) : zéro initial, espaces, tirets et préfixe `00` retirés, un indicatif déjà présent (le nôtre ou celui d'un autre pays) n'est jamais réécrit. Normalisation appliquée à l'ajout d'une dette et défensivement au moment de l'envoi (numéros importés/anciens). Message de relance désormais modifiable avant envoi (WhatsApp et SMS) dans `DetteDetailSheet`. Repli automatique sur SMS si l'ouverture de WhatsApp échoue. `flutter analyze` (0 erreur) et `flutter test` (14 tests, tous au vert) passent. **Pas encore testée sur téléphone.**
- **Phase 7** — terminée (code). « Bénéfice net » renommé partout en **marge brute** (`ItemVendu.margeBrute`, `Transaction.margeBrute`/`margeBruteMineur` — renommage sûr, les index `@HiveField` 4 et 8 sont inchangés) pour ne plus laisser croire qu'il s'agit d'un résultat après charges. Nouveau carnet de **dépenses** : modèle `Depense` (typeId 9) et enum `CategorieDepense` (typeId 8 : loyer, transport, électricité, réapprovisionnement, autre), `DepenseService`/`DepenseProvider`, inclus dans l'export/import de sauvegarde (avec repli de compatibilité pour les anciennes sauvegardes sans dépenses). Nouvel onglet **Rapport** (5ᵉ onglet de la navigation) : sélecteur Jour / Semaine / Mois (`lib/utils/periodes.dart`) et tableau de bord CA, marge brute, dépenses, résultat net (marge brute − dépenses, en rouge si négatif), avec liste des dépenses de la période et suppression. Les méthodes de calcul par période (`CaisseService.totalMineurSur`/`margeBruteMineurSur`) sont entièrement nouvelles, ajoutées à côté des méthodes existantes du jour utilisées par l'écran Caisse — aucune des méthodes ni des tests déjà en place n'a été touchée. `flutter analyze` (0 erreur) et `flutter test` (21 tests, tous au vert) passent. **Pas encore testée sur téléphone.**
- **Phase 8** — terminée (code). `google_mobile_ads` avait déjà été retiré du projet lors de la résolution du crash au démarrage (voir plus haut) : le build web ne casse donc plus pour cette raison. Ajout de `lib/config/feature_flags.dart` (`pubsActivees`, `false` par défaut) : `AdService.initialiser()`/`afficherInterstitielSiOpportun()` sortent immédiatement si le drapeau est désactivé — point d'activation unique pour le jour où une vraie implémentation publicitaire sera réintroduite. `.github/workflows/build_apk.yml` : nouveau job `build_web` (parallèle, indépendant des jobs APK existants) qui exécute `flutter build web --release` et publie l'artefact — c'est GitHub Actions qui vérifie l'objectif de sortie de cette phase, jamais la machine locale (règle 9, généralisée : aucun `flutter build` local, quelle que soit la plateforme). `flutter analyze` (0 erreur) passe. **Pas encore testée sur téléphone ni en web.**
- **Phase 9** — terminée (code, préparation uniquement). L'ancien `SyncService` ne poussait qu'à sens unique vers Firestore, sans restauration possible : ce n'était pas une vraie sauvegarde. L'architecture d'une synchronisation **bidirectionnelle** est posée, mais rien n'est activé — Firebase reste dehors tant que le propriétaire n'a pas lancé `flutterfire configure` (règle 8/6 : aucun plugin natif sans validation explicite, et celui-ci a déjà causé un crash réel). Ajouts, tous additifs (aucun `@HiveField` existant renuméroté) : `deviceId` (String) et `lastModified` (DateTime?) sur `Produit`, `Transaction`, `Dette`, `PaiementDette` et `Depense`, renseignés à la création et à chaque mutation (`ProduitService.ajouter/modifier/decrementerStock/reapprovisionner`, `DetteService.ajouter/enregistrerPaiement`, `DepenseService.ajouter`, `CaisseService.finaliserVente/annulerTransaction`) via le nouveau `DeviceIdService` (UUID stable persisté dans les paramètres). Nouveau modèle `ConflitSynchronisation` (typeId 10, box dédiée) : journal des conflits, vide tant qu'aucune synchronisation réelle n'existe. Nouveau `SyncConflictResolver` : logique pure de résolution « dernier écrit gagne », testée indépendamment de tout transport réseau. `SyncService` reste un no-op complet tant que le nouveau drapeau `syncActivee` (`lib/config/feature_flags.dart`, `false` par défaut, non modifiable depuis l'app) n'est pas activé en dur dans le code. `flutter analyze` (0 erreur) et `flutter test` (27 tests, tous au vert) passent. **Pas encore testée sur téléphone** — sans objet ici, aucun comportement visible n'a changé.
- **Phase 10** — terminée. Couverture de tests minimale complétée par rapport à la liste de `docs/PHASES.md` : `test/money_test.dart` (arrondis, devise 0 décimale/2 décimales, aller-retour unités mineures) ; `test/caisse_service_test.dart` (`finaliserVente` sur quantités multiples avec prix figés — un changement de prix après-coup ne modifie jamais une transaction déjà enregistrée — et rejet du panier vide) ; `test/dette_service_test.dart` (normalisation E.164 à l'ajout, génération du message de relance, paiement partiel) ; `test/backup_service_test.dart` (export puis import en mode remplacement, aller-retour sans perte sur produits/ventes/dettes/paiements/dépenses) ; `test/panier_encaissement_widget_test.dart` (test de widget bout en bout : ajout au panier par un vrai tap sur la carte produit, puis encaissement via `CaisseProvider.finaliserVente`, vérifie la transaction créée et le stock décrémenté — la validation par les boutons de la feuille modale n'est pas simulée telle quelle : l'enchaînement de routes animées `showModalBottomSheet` + `showDialog` ne se stabilise jamais sous `pumpAndSettle` en environnement de test ; note technique retenue : toute écriture Hive réelle dans le corps d'un `testWidgets` doit passer par `tester.runAsync()`, sans quoi elle reste bloquée indéfiniment — contrairement à `setUp`/`tearDown`, qui s'exécutent hors de la zone fake-async de `testWidgets` et où les mêmes écritures fonctionnent sans précaution particulière). `flutter analyze` (0 erreur) et `flutter test` (36 tests, tous au vert) passent. `CHECKLIST.md` revu et à jour. C'était la dernière phase de `docs/PHASES.md` — voir le récapitulatif de fin de session pour ce qu'il reste à vérifier et à faire manuellement.
- **Conditionnements (produits vendus en plusieurs formats)** — terminée (code). N'était pas dans `docs/PHASES.md` initial : un même produit se vend en gros et à l'unité (ex: un casier de Coca de 20 bouteilles vendu entier ou à la bouteille), à des marges différentes, ce que le schéma ne représentait pas avant cette phase. Nouveau modèle `Conditionnement` (typeId 11, box dédiée `conditionnements_box`) : `nom`, `quantiteEnUniteBase`, `prixVente`/`prixVenteMineur`, `parDefaut`. Le stock (`Produit.stockActuel`) et le prix d'achat (`Produit.prixAchat`/`prixAchatMineur`) sont désormais toujours exprimés en **unité de base** (nouveau champ additif `Produit.uniteBase`, `@HiveField(13)`, défaut `« unité »`) — un conditionnement n'est qu'un multiplicateur de vente vers cette unité. `ItemVendu` fige au moment de la vente le format vendu (`conditionnementNom`, `quantiteEnUniteBase`, nouveaux `@HiveField` 7-8, défaut `''`/`1`) : modifier ou supprimer un `Conditionnement` plus tard ne change jamais une vente déjà enregistrée. `CaisseService.finaliserVente`/`annulerTransaction` décrémentent/réapprovisionnent désormais `item.quantite * item.quantiteEnUniteBase` (au lieu de `item.quantite` seul) — comportement strictement inchangé pour un produit à conditionnement unique puisque `quantiteEnUniteBase` vaut 1 par défaut. Nouveau `ConditionnementService` (CRUD, `remplacerPour` pour l'édition en bloc depuis le formulaire, `conditionnementSynthetique` qui calcule un format « quantité 1 » à la volée sans rien écrire pour l'affichage d'un produit encore sans conditionnement, `migrerSiAbsent` qui le persiste réellement — utilisé uniquement à l'import d'une sauvegarde) et `ConditionnementProvider` (enregistré dans `main.dart`). Écran Stock (`produit_form_sheet.dart`, réécriture complète) : unité de base avec suggestions, calculateur d'achat (« j'achète par [casier] de [20] [bouteilles] au prix de [1500] » → prix unitaire calculé et affiché), liste éditable des formats de vente avec marge calculée en direct et sélection du format par défaut, stock en unité de base avec raccourci « j'ai [n] [format] ». Liste du Stock : stock affiché avec conversion lisible (« 47 bouteilles (2 casiers + 7) »). Écran Caisse : une touche ajoute le format par défaut (aucune régression pour un produit à conditionnement unique), un appui long ouvre le choix du format pour un produit qui en a plusieurs (icône discrète sur la tuile), panier affichant le format vendu par ligne. `BackupService` exporte/importe la nouvelle box (`formatVersion` 1 → 2) ; une sauvegarde au format v1 reste importable, chaque produit qui y arrive sans conditionnement en reçoit un automatiquement (quantité 1, prix de vente importé). `flutter analyze` (0 erreur) et `flutter test` (42 tests, tous au vert — 6 nouveaux fichiers `test/conditionnement_*_test.dart`, un test par fichier comme le reste de la suite : `HiveService` a un verrou d'initialisation statique par isolate, donc plusieurs `test()` dans un même fichier partageant un seul `main()` provoquent un `HiveError: Box not found` à partir du deuxième test) passent. **Pas encore testée sur téléphone.**
- **Design** — non commencé. Les polices `.ttf` sont sur le disque mais non suivies par Git, et la section `fonts:` a été retirée de `pubspec.yaml`.

---

## Règles permanentes

1. **Ne jamais recréer le projet.** Pas de `flutter create`, pas de suppression de dossier. Travail incrémental uniquement.
2. **Ne jamais changer un `typeId` Hive existant ni renuméroter un `@HiveField`.** On ne peut qu'ajouter de nouveaux champs, avec de nouveaux index, nullables ou avec valeur par défaut.
3. **Aucune publicité sur l'écran Caisse**, sous aucune forme. La rapidité de vente est le cœur du produit.
4. **Aucun montant affiché sans passer par le formateur central.** Jamais de « 1 500,00 FCFA ».
5. **Phases enchaînées.** À la fin de chaque phase : `flutter analyze` à zéro erreur, `flutter test` au vert, mise à jour de l'État actuel, commit, push — puis **enchaîner directement sur la phase suivante sans s'arrêter**. À la toute fin (dernière phase traitée dans la session), donner la liste complète et précise de ce que le propriétaire doit vérifier sur son téléphone pour chaque phase livrée.
6. **Aucun nouveau plugin natif sans validation explicite.** Flutter 3.44 casse plusieurs plugins qui n'ont pas suivi la migration Built-in Kotlin. En cas de doute, préférer du Dart pur ou un paquet maintenu par l'équipe Flutter.
7. **Avant chaque push : `git status`.** Tout ce que `pubspec.yaml` référence doit être suivi par Git. La CI ne voit que ce qui est commité — une déclaration d'asset sans le fichier casse le build.
8. **Firebase reste dehors** jusqu'à la Phase 9, et uniquement après que le propriétaire ait lancé `flutterfire configure`. Le fichier `google-services.json` présent est inerte et ne doit pas être commité.
9. **Compilation locale interdite, sans exception.** `flutter analyze` et `flutter test` sont autorisés en local, ils sont légers. Tout le reste — `flutter build apk`, `flutter build appbundle`, `flutter build web`, toute tâche Gradle, tout `flutter build <cible>` — est interdit, quelle que soit la plateforme, même quand une phase liste un objectif de sortie du type « `flutter build web --release` → OK » : la machine n'a que 4 Go de RAM et un build ne termine jamais (il tourne pendant une heure puis rien, ce n'est pas récupérable). Les artefacts de build sont produits uniquement par GitHub Actions. Procédure : `flutter analyze` → `flutter test` → commit → push — et c'est GitHub qui construit. Impossible de vérifier soi-même qu'un plugin natif ou une cible de build compile ; on pousse et on attend le retour du propriétaire après consultation du build sur GitHub.

---

## Environnement

- Le propriétaire **ne peut pas compiler en local** : sa chaîne Android est cassée. Tous les APK sont produits par **GitHub Actions** (`.github/workflows/build_apk.yml`), qui génère trois artefacts : `minimal`, `debug`, `release`.
- Il n'a **ni `adb` ni logcat**. Tout diagnostic doit être visible sur l'écran du téléphone.
- `lib/main_minimal.dart` est un point d'entrée de diagnostic qui n'affiche qu'un écran uni. Il permet de distinguer un problème natif d'un problème Dart. Le conserver.

---

## Marché

Afrique de l'Ouest, francophone. Devise principale : le franc CFA, **sans décimale**. Les relances de dettes passent par WhatsApp. Toute l'interface est en français.